import 'package:flutter/material.dart';

class SearchPicker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Dialog(
        child: Container(
      height: 300,
      child: Text("hellow"),
    ));
  }
}
